import logo from '../../assets/images/logo.png';

const Logo = () => {
    return (
        <div className='logo-image'>
            <img src={logo} alt='Logo' />
        </div>
    );
};

export default Logo;
